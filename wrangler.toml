name = "radovanpavelko"
compatibility_date = "2024-01-01"

# Cloudflare Pages configuration
# Build command: npm run build
# Build output directory: dist
# Node.js version: 20 (recommended)

[env.production]
# Set production-specific vars here if needed
